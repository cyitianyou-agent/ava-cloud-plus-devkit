# 安全策略

## 支持范围

安全修复优先发布到最新版本和 `main` 分支。

## 报告安全问题

请不要在公开 Issue 中提交凭据、客户报表、数据库连接信息或未脱敏业务数据。优先使用 GitHub Security Advisory 的私密报告功能；如果仓库尚未启用该功能，请联系维护者 `cyitianyou@gmail.com`，只描述复现所需的最少信息。

## 数据边界

本插件本身不声明 MCP 服务或外部应用连接。各 Skill 处理的项目文件仍受 Codex 当前沙箱、授权和用户指令约束。Crystal Reports 转换流程要求先移除连接串、服务器、保存数据和参数当前值。
